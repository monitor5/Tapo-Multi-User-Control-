# Tapo Control Environment Configuration
# Copy this to .env and configure your values
# SSL/HTTPS is handled by Synology Domain Certificate

# ── App Settings ──────────────────────────────────────────
APP_ENV=development          # development or production
DEBUG=true                   # true for dev, false for prod
PORT=5011

# ── Security ───────────────────────────────────────────────
# Generate with: openssl rand -hex 32
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# ── Tapo Device Credentials ───────────────────────────────
TAPO_EMAIL=your-tapo-account@email.com
TAPO_PASSWORD=your-tapo-password

# Device configuration (JSON format)
PLUGS={"집컴": "192.168.45.179", "fakeplug": "192.168.0.1"}

# ── Admin User ─────────────────────────────────────────────
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password

# ── Synology Deployment Settings ──────────────────────────
# HTTP port for nginx container (Synology reverse proxy will handle HTTPS)
NGINX_HTTP_PORT=84           # Alternative HTTP port to avoid conflicts

# Development database admin port
ADMINER_PORT=8081            # Alternative port for dev database admin 